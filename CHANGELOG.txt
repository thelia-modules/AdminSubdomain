# 0.2.4

- Add compatibility with TinyMCE Module.

# 0.2.3

- Add ignore the treatment if the response is of binaries type or streamed type. Thanks @bilhackmac !

# 0.2.2

- Fix bug, when Thelia is already in a subdomain. The subdomain was not kept in the administration URLs.

# 0.2.1

- Fix bug, when the subdomain url has no path. An automatic redirection on the administrator panel is now performed.

# 0.2.0

- Adds the case or Thelia is already on a subdomain
- This module is compatible with the BackOfficePath Module